# 🏠 Dotfiles

My personal dotfiles for Linux (Ubuntu/GNOME) development environment.

**Note**: These dotfiles are tailored for my personal workflow on Ubuntu with GNOME. You may need to adjust paths and settings for other distributions or desktop environments.

```
bash setup.sh
```
